{
  "name": "my-vite-cv",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "vite build && cd dist && git add . && git commit -m \"deploy to gh-pages\" && cd .. && npx push-dir --dir=dist --branch=gh-pages"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.4.0",
    "autoprefixer": "^10.2.5",
    "cssnano": "^5.0.1",
    "postcss": "^8.2.12",
    "push-dir": "^0.4.1",
    "tailwindcss": "^2.1.1",
    "vite": "^2.1.5"
  }
}